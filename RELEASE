# ./RELEASE
# chartix release list

--------------------------------------------------------------------------------
@ gui_lib (v0.04)
~ Layers don't work. In the future they will definitely be.

> Delete Layers;
> Fix `stack overflow`. Possibly (=_=)

--------------------------------------------------------------------------------
@ game (nil)
--------------------------------------------------------------------------------